UnauthorizedError:
  description: 인증 실패
  content:
    application/json:
      schema:
        type: object
        properties:
          success:
            type: boolean
            example: false
          message:
            type: string
            example: '인증이 필요합니다.'

ServerError:
  description: 서버 오류
  content:
    application/json:
      schema:
        type: object
        properties:
          success:
            type: boolean
            example: false
          message:
            type: string
            example: '서버에서 오류가 발생했습니다.'

