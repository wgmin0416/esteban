paths:
  /my-info:
    get:
      tags: [user]
      summary: 내 정보 조회
      security:
        - BearerAuth: []
      responses:
        200:
          description: 사용자 정보
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        401:
          $ref: '#/components/responses/UnauthorizedError'
